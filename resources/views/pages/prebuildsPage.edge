@vite('resources/css/prebuildsPage.css')

@layouts.documentationLayout({ title: "Prebuilds" })

@if(planterConfigs)
<div class="configList">

  @each(config in planterConfigs)

  @!component('components/configCard', { configDetail: config, admin: false })

  @end
</div>
@end

@end